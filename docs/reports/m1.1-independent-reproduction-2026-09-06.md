# M1.1 independent automated reproduction — 2026-09-06

## Verdict

Technical PASS at `e2a6a0f29bff9bd720433fefaebd4c4a26bbff2c` from a fresh clone of the
public GitHub repository. The reproducer used only the public repository and issue #3. It did not
inspect or reuse an existing Variaxiom checkout, virtual environment, Cargo target, or build cache.

The public evidence is recorded in
[issue #3](https://github.com/artemiosu/variaxiom/issues/3#issuecomment-5572672996), which was closed
as completed after publication.

This is independent automated reproduction. It does not substitute for the separate three-role
security-council review required before implementing M2.1 or for production security assurance.

## Environment

- Ubuntu 24.04.4 LTS, Linux 6.8.0-136, x86_64;
- CPython 3.13.15; host `python3` 3.12.3;
- uv 0.12.5;
- rustc/cargo 1.98.1 and rustup 1.29.1;
- Git 2.43.0 and Bash 5.2.21.

The repository-recorded M1.1 implementation revision is
`b0624843a0c21566395828ac50ca9e7382473807`. The tested HEAD adds only documentation,
project-context, and project-schema evidence; it does not change Python, Rust, fixtures, or CI.

## Commands

```text
git clone https://github.com/artemiosu/variaxiom.git repo
cd repo
bash scripts/bootstrap.sh
bash scripts/verify.sh
cargo fmt --all -- --check
cargo test --workspace --all-targets --locked
cargo clippy --workspace --all-targets --locked -- -D warnings
git status --porcelain=v1 --untracked-files=all
```

## Results

| Check | Result | Time |
|---|---:|---:|
| Bootstrap | PASS | 2.976 s |
| Complete repository verification | PASS | 8.426 s |
| Rust format | PASS | 0.284 s |
| Rust tests | PASS | 17.710 s |
| Rust Clippy, warnings denied | PASS | 7.033 s |

- Python: 23/23 tests passed.
- Rust: 17/17 tests passed: 7 kernel, 1 shared promotion-conformance, 9 protocol.
- All eight shared promotion fixtures matched exact Python/Rust decisions, canonical bytes, and
  digests.
- The final Git status was empty. Only ignored environments, caches, and build output were created.
- The public Linux/macOS/Windows
  [CI run](https://github.com/artemiosu/variaxiom/actions/runs/34062266427) passed at the tested
  commit.

## Authority Test

- `candidate:slug-tool-unbounded` was rejected with
  `authority.not_granted:network.unrestricted`.
- `candidate:slug-tool-bounded` was accepted with `promotion.accepted`.
- Ledger: valid, 14 events.
- Ledger head: `9cbe298edea1d7b03cf3bc175dd8a2ec525ec545e9be4625d5a6d565238e3a13`.
- Ledger file SHA-256: `c15e3f9a23f06ef258081693086d7d2acaedc4abb76d64b01ac1771b3fa04595`.

Two fresh output directories produced byte-identical ledgers and HTML. JSON reports differed only
in the intentionally recorded output-directory path. Repeated runs using the same output path were
byte-identical for ledger, JSON, and HTML. The generated files matched `docs/demo` byte-for-byte.

## Deviations

- A restricted-sandbox attempt could not write its global uv cache; normal cache access passed.
  This was an execution-environment restriction, not a project prerequisite.
- `/usr/bin/time` was unavailable, so Bash's built-in timer supplied timings.
- Complete report JSON intentionally records the selected workspace path; the ledger and documented
  reproducibility claim are unaffected.
